events {}

http {
    upstream api {
        server api:8000;
    }

    upstream frontend {
        server frontend:4173;
    }

    server {
        listen 80;

        location /api/ {
            proxy_pass http://api/;
        }

        location / {
            proxy_pass http://frontend/;
        }
    }
}
